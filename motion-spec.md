# 动效：目的、分镜、触发、规格

## 先写目的，再定时长

每段运动只保留一个主要目的：

| 目的 | 示例 | 应观察的结果 |
|---|---|---|
| 反馈 | 点击后按钮状态改变 | 用户知道操作已被接收 |
| 关系 | 卡片展开为详情，保留对应关系 | 用户知道新内容来自哪里 |
| 解释 | 依次呈现输入与结果 | 用户理解过程，而非误认为同步完成 |
| 注意力 | 重要信息出现时短暂强调 | 用户看到重点，之后能继续阅读 |
| 品牌表达 | 关键场景采用一致运动节奏 | 形成可辨认体验，同时保持可用 |

任务效率优先用 productive motion；重要时刻再用 expressive motion。始终准备静态或简化替代方案。

### 节奏

- 每段明确一个主要视觉焦点  
- 相关元素成组运动  
- 留出稳定阅读时间  
- 长距离位移与复杂转场单独验证  
- 不要给所有按钮、正文和场景套同一时长  

若删掉某段后信息与品牌都不损，就简化。全站持续运动会让关键时刻失去区分。

## 先画分镜

每一拍写清：

**初始画面 → 用户动作／触发 → 发生变化 → 最终画面 → 用户理解了什么**

网页还需补充电影分镜常缺的行为：

- 快速滚过  
- 反向滚动  
- 动画中途点击  
- 刷新  
- 窗口改变  
- 重复进入  
- 动画资源加载失败  

观看样片只能评节奏；上述行为必须在可操作原型中验证。

## 三种触发（不要混写）

| 类型 | 含义 | 必须定义 |
|---|---|---|
| 时间驱动 | 触发后按时间播放 | 是否重播？能否跳过/停止？ |
| 滚动联动 | 状态随滚动进度改变 | 起止位置、反向、手机替代 |
| 事件／状态 | 点击、展开、加载成功等 | 成功、失败、中断、连续操作 |

不要把「进入视口后播放一次」和「进度跟随滚动」都写成「滚动动画」。

## 动效规格表（交接必填）

以下时间与距离仅为格式示例，需在真实设备调校。

| 字段 | 示例 |
|---|---|
| 名称与目的 | 场景说明出现；帮助访客关联图像与说明 |
| 元素 | 场景图、说明文本；文本在语义结构中始终存在 |
| 触发 | 首次进入约定可视区域后播放一次 |
| 初态与终态 | 装饰图轻微位移至静止；核心标题默认可见 |
| 时长与延迟 | 240ms，无额外等待（示例） |
| 缓动 | 起步较快、结束减速；交付命名 token 或确定曲线 |
| 多元素关系 | 图与说明同步，不逐字延迟阅读 |
| 重入／中断 | 不重复播放；快速滚过时直接呈现终态 |
| 手机 | 缩小位移；必要时取消固定长滚动区 |
| 减少动态效果 | 无位移，直接呈现完整内容 |
| 加载失败 | 保留文字和可用替代图，不等待特效 |
| 验证 | 快速与反向滚动、重复进入、触屏和键盘 |

复杂动画再补充：坐标参照、变换中心、遮罩范围、层级、滚动区间、资源路径、控制按钮、尺寸变化后的重算。

## 无障碍与性能底线

- 自动开始、持续超过约 5 秒且与其他内容并行的运动，通常需暂停/停止/隐藏（WCAG 2.2.2；必要活动有例外）。  
- 建议支持 `prefers-reduced-motion` / 可关闭非必要交互动画（2.3.3 为 AAA；仍应交付减少动态方案）。  
- 优先动画 transform / opacity；仍需实测——大面积合成层与媒体仍有成本。  
- Web Vitals「良好」参考（第 75 百分位，移动与桌面分开）：LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1。实验室找问题，上线后结合真实用户数据。Vitals 不能完整衡量动效观感。

## 按风险选原型保真度

| 不确定点 | 最小验证产物 |
|---|---|
| 用户看不懂价值 | 内容草稿或线框，听复述 |
| 风格不贴品牌 | 相同文案的关键视觉对比 |
| 节奏是否舒适 | 短样片，再做可操作原型 |
| 滚动是否失控 | 浏览器原型；快速与反向滚动 |
| 特效手机能否跑 | 真实素材的局部技术样例 + 目标设备 |

测试任务要具体（如「找出适用场景并进入完整演示」）。记录成败、卡点、误解与设备条件。小样本用于发现问题，不据此宣称转化提升。修改后复测受影响任务。
